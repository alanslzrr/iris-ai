"use client"

import * as React from "react"
import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio"

// Thin wrapper so we can centralize future styling if needed
const AspectRatio = AspectRatioPrimitive.Root

export { AspectRatio }


